# Dans le dossier de votre projet, créez un fichier .gitignore s'il n'existe pas déjà
echo "node_modules
build
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*" > .gitignore

# Initialisez Git s'il n'est pas déjà initialisé
git init

# Ajoutez tous les fichiers au staging
git add .

# Faites votre premier commit
git commit -m "Version initiale de TimeTrack"
node_modules
